### 设置最大最小宽高
```dart
    Container(
        constraints: BoxConstraints(
            minWidth: 180,  // 最大高度
            minHeight: 50,  // 最小高度
        ),
        child: Text('Container设置最小宽高'),
    )
```